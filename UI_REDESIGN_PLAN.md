# UI Redesign Plan

1. Bundle the supplied design references and load only project-local assets.
2. Centralize Light, Dark, AMOLED, and Cyberpunk tokens in `app.py`.
3. Apply shared ttk styles and lightweight Tk/Canvas helpers without moving tabs or workflows.
4. Use supplied logo variants per theme; no recreated wordmark in the header.
5. Restyle existing screens through shared helpers, dynamic button classification, modern logs, tables, tabs, scrollbars, and safety states.
6. Verify compile, self-test, bundled assets, and live theme switching.
